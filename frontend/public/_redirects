# Specific auth routes (these will be handled by React Router)
# IMPORTANT: Specific routes must come before the catch-all rule
/auth/*    /index.html   200
/signin    /index.html   200
/signup    /index.html   200

# SPA routing redirects - catch-all rule (must be last)
/*    /index.html   200
